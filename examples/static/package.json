{
	"name": "examples-static",
	"version": "0.1.0",
	"main": "index.js",
	"license": "MIT",
	"private": true,
	"scripts": {
		"dev": "yarn build-server && node server/examples/static/server-src/index.js",
		"build-server": "rimraf server && tsc -p server-src/tsconfig.json"
	},
	"dependencies": {
		"dotenv": "^8.2.0",
		"serve-handler": "^6.1.2",
		"typescript": "^3.8.3"
	},
	"devDependencies": {
		"@types/serve-handler": "^6.1.0"
	}
}
